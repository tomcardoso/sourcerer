# Suppress noise in CI logs
fund=false
audit=false
# electron-vite 2.x (esbuild 0.21) and vitest 4.x (esbuild 0.27+) have an
# incompatible peer dep on esbuild; use legacy resolution to keep a flat tree
legacy-peer-deps=true
